import React, { useState } from 'react'

function ShowChatAndPdf({file, fileName, fileSize}: {file: File, fileName: string, fileSize: string}) {

  return (
    <div>ShowChatAndPdf</div>
  )
}

export default ShowChatAndPdf